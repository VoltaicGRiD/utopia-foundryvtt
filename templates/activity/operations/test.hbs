<div>
  <hr/>
  <span class="operation-category">Perform a test (vs target(s)):</span>
  
  <div class="operation-content-row">
    <div class="operation-content">
      <label for="testType">Executor Check:</label>
      <select name="executorTest" required>
        {{#each choices as |group|}}
          
          <optgroup label="{{group.name}}">
            {{#each group.choices as |name id|}}
              <option value="{{id}}" {{#if (eq ../../operation.executorTest id)}}selected{{/if}}>{{localize name.label}}</option>
            {{/each}}
          </optgroup>
        {{/each}}
      </select>
    </div>

    <div class="operation-content">
      <label for="testType">Target Check:</label>
      <select name="targetTest" required>
        {{#each choices as |group|}}
          
          <optgroup label="{{group.name}}">
            {{#each group.choices as |name id|}}
              <option value="{{id}}" {{#if (eq ../../operation.targetTest id)}}selected{{/if}}>{{localize name.label}}</option>
            {{/each}}
          </optgroup>
        {{/each}}
      </select>
    </div>
  </div>  

  <div class="operation-content-row">
    <div class="operation-content">
      <label for="targetTestFormula">Target Check Formula:</label>
      <input type="text" name="targetTestFormula" value="{{operation.targetTestFormula}}" placeholder="" required>
      <span class="hint">The formula to be used for the target check. This formula will be used to determine the target's roll. This formula is ignored, unless "Target Check" is set to "Formula".</span>
    </div>

    <div class="operation-content">
      <label for="continueExecution">Continue execution on successful tests:</label>
      <input type="checkbox" name="continueExecution" {{#if operation.continueExecution}}checked{{/if}}/>
    </div>
  </div>

  <div class="operation-content-row">
    <div class="operation-content">
      <label for="overrideRoll.enabled">Override roll:</label>
      <input type="checkbox" name="overrideRoll.enabled" {{#if operation.overrideRoll.enabled}}checked{{/if}}/>
    </div>

    <div class="operation-content">
      <label for="overrideRoll.ifLessThan">If less than:</label>
      <input type="number" name="overrideRoll.ifLessThan" value="{{operation.overrideRoll.ifLessThan}}"/>
    </div>

    <div class="operation-content">
      <label for="overrideRoll.setTo">Set to:</label>
      <input type="number" name="overrideRoll.setTo" value="{{operation.overrideRoll.setTo}}"/>
    </div>

    <div class="operation-content">
      <label for="overrideRoll.beforeModifiers">Before modifiers:</label>
      <input type="checkbox" name="overrideRoll.beforeModifiers" {{#if operation.overrideRoll.beforeModifiers}}checked{{/if}}/>
    </div>
  </div>

</div>